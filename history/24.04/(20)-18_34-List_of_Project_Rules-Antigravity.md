User request: "list your rules"

### 2. Objective Reconstruction
The user requested a comprehensive list of the rules that govern the AI's behavior and operational procedures within this project.

### 3. Strategic Reasoning
I performed a directory listing of `.agents/rules/` to ensure I captured the full set of active protocols. I then synthesized the purpose of each rule into a concise table for high readability, adhering to the `global-behavior.md` principle of being "Short, Precise, and Simple."

### 4. Detailed Blueprint
- List files in `.agents/rules/`.
- Cross-reference with the `GEMINI.md` source of truth.
- Summarize the intent of each rule.
- Present the summary to the user.
- Log the interaction.

### 5. Operational Trace
- Executed `list_dir` on `.agents/rules/`.
- Verified that all 8 rules found in the directory were relevant.
- Generated the response table.

### 6. Status Assessment
The user is now informed of the active governance structure. No further action is required until the next command.
