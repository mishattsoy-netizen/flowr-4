User request: "put timer in the card with settings and actions on the right side"

## Objective Reconstruction
The user requested to move the `Next run in` timer from the header area of the Routine page into the Controls settings and actions bento card on the right side.

## Strategic Reasoning
We moved the timer next to the "LIVE" indicator inside the settings/actions card, using `ml-auto` to push it to the right. This consolidates all dynamic controls and countdown status into a single cohesive UI element.

## Detailed Blueprint
- **Modified**: `src/app/admin/bot/routine/RoutineClient.tsx`
  - Removed timer from page header.
  - Placed timer in the Controls card aligned to the right.

## Operational Trace
- Positioned and checked responsiveness using `flex-wrap`.

## Status Assessment
Timer moved successfully and displays accurately on the right side of the Controls card.
