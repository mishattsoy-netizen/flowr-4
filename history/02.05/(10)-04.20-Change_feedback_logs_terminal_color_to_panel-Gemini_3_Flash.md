User request: "change terminal color to panel"

## Objective Reconstruction
The user requested to change the background color of the log stream terminal box within the Feedback Logs page to match the rest of the application's bento panel style (`bg-panel`).

## Strategic Reasoning
We replaced the ad-hoc dark background class `bg-[#0a0d14]` with the custom `bg-panel border border-white/5` class tokens for visual consistency.

## Detailed Blueprint
- **Modified**: `src/app/admin/bot/feedback/FeedbackClient.tsx`
  - Restyled log stream background to use `bg-panel border border-white/5`.

## Operational Trace
- Verified that the logs box perfectly matches the dashboard's design aesthetics.

## Status Assessment
Terminal logs box color successfully updated.
