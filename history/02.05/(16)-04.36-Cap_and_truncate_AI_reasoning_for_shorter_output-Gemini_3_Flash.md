User request: "make ai reasoning shorter and easier to read"

## Objective Reconstruction
The user requested to make the AI reasoning box's text even shorter, more direct, and easier to read to prevent a large wall of text from overwhelming the interface.

## Strategic Reasoning
We introduced a strict length limit and truncation strategy on the server:
1. Updated the prompt to explicitly instruct the model to produce a reasoning summary of max 50 words without role descriptions or metadata.
2. Added defensive server-side truncation that limits reasoning text to the first 3 sentences or up to 250 characters, stripping any accidental prompt repetitions.

## Detailed Blueprint
- **Modified**: `src/app/api/ai/brain/analyze/route.ts`
  - Restructured instruction to impose a 50-word cap on reasoning text.
  - Implemented sentence and length-based truncation on the server to prevent any raw AI slop from entering the UI.

## Operational Trace
- Successfully verified that long output text is capped and truncated at 250 characters.

## Status Assessment
AI Reasoning output size and scannability significantly improved.
