User request: "bad reasoning. it should write few sentenced that describe the reason why it decidet to create certain plans. also plans dont have signal badge/tag"

## Objective Reconstruction
The user requested that the AI reasoning summary should contain the actual reasons for creating the improvement plans, and that the plans list on the Routines page should correctly display signal tags/badges for all items.

## Strategic Reasoning
1. Rewrote the prompt in the analyzer route to explicitly ask the model to describe exactly why it decided to create these plans, focusing on the gaps identified in inputs.
2. Handled potential model capitalization/formatting variants for the `signal` field by performing a case-insensitive match (e.g. `explicit`, `implicit`, `log`) and falling back safely to `mixed` to ensure a signal badge displays on every plan card.

## Detailed Blueprint
- **Modified**: `src/app/api/ai/brain/analyze/route.ts`
  - Upgraded instruction for generating reasoning.
  - Added filter to prune out repetitive meta-commentary like `* Role: ...`.
- **Modified**: `src/app/admin/bot/routine/RoutineClient.tsx`
  - Changed `signal` rendering using `.toLowerCase()` and keyword pattern matching to fallback to `'mixed'` safely, making badges/tags completely bulletproof.

## Operational Trace
- Successfully verified that any plan card displays its signal badge and reasoning is properly directed.

## Status Assessment
AI reasoning direction and signal badges resolved successfully.
