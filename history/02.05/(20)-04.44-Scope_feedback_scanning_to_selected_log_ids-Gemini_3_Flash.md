User request: "when i select one or multiple feeback and write MANUAL CLARIFICATION / OVERRIDE and reanalyze, it must only run analysis with context for selected feedabcks"

## Objective Reconstruction
The user requested that when analyzing with specific feedback entries selected (with or without a directive override), the analysis is strictly scoped to the context of those selected feedbacks instead of pulling all global feedback from the database.

## Strategic Reasoning
We updated the feedback scanner in the analysis API route (`analyze/route.ts`). If the user specifies `log_ids` (via clicking Send to Analysis), the `message_feedback` query is constrained to only fetch feedback records matching the provided `message_log_id` in the selected list.

## Detailed Blueprint
- **Modified**: `src/app/api/ai/brain/analyze/route.ts`
  - Scoped the `message_feedback` query to filter by `message_log_id` matching the incoming `logIds`.

## Operational Trace
- Successfully verified that global feedback is not mixed into targeted analysis sessions.

## Status Assessment
Feedback scanning correctly scopes down to user-selected items.
