User request: "also show small logo/site favicon in the small sqare and make pills a bit smaller"

## Objective Reconstruction
The user requested to add a site favicon image to each search source pill, inside a small square, and make the pill sizes a bit more compact.

## Strategic Reasoning
We extracted the URL hostname and resolved the favicon using Google's public favicon API (`https://www.google.com/s2/favicons`).
We rendered this inside a tiny rounded square (`rounded-[4px]`) within the pill layout, and adjusted the pill padding to make them visually lighter.

## Detailed Blueprint
- **Modified**: `src/components/assistant/components/ChatMessage.tsx`
  - Added the favicon square and reduced pill padding to `px-2 py-0.5`.

## Operational Trace
- Successfully tested the anchor renderer to ensure it falls back gracefully when the URL or favicon is missing.

## Status Assessment
Pills now include a small square with the site favicon, and have a more compact size.
