User request: "use bnoe 60 for site names"

## Objective Reconstruction
The user requested to use the bone-60 (`var(--bone-60)`) color token for the site names displayed within source pills.

## Strategic Reasoning
We updated the anchor tag styling inside the `markdownComponents` to use `text-[var(--bone-60)] hover:text-accent` to apply bone-themed color tokens consistently while maintaining a nice hover transition.

## Detailed Blueprint
- **Modified**: `src/components/assistant/components/ChatMessage.tsx`
  - Replaced `text-accent` with `text-[var(--bone-60)] hover:text-accent`.

## Operational Trace
- Verified that the color applies perfectly and hovers gracefully.

## Status Assessment
Site names in pills now consistently use bone-60.
