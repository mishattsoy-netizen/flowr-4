User request: "make legent more compact"

## Objective Reconstruction
The user requested to make the Plan Origin and Trigger Legend much more compact.

## Strategic Reasoning
We transitioned the legend layout from a multiline two-column grid into a dense, high-efficiency horizontal flex-wrap container. We reduced the padding from `py-3` to `py-2`, shrunk down the label text and icon sizes slightly, and made the descriptions flow inline with the badges.

## Detailed Blueprint
- **Modified**: `src/app/admin/bot/routine/RoutineClient.tsx`
  - Replaced the grid based legend with a single-line or wrapped flex container.

## Operational Trace
- High-density legend rendered successfully.

## Status Assessment
Compact, space-efficient legend implemented successfully.
