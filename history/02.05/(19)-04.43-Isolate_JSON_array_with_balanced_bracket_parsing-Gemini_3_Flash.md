User request: "fix"

## Objective Reconstruction
The user experienced an error while parsing the AI's response: `Error: Unexpected non-whitespace character after JSON`. This happens when the AI includes trailing thoughts or markdown formatting after the JSON array.

## Strategic Reasoning
To extract exactly the JSON array and ignore any subsequent commentary, we implemented a balanced bracket counting function. Starting from the first `[`, it increments for every `[` and decrements for every `]` until the count returns to zero. This precisely isolates the JSON array content, avoiding greedy match errors or parsing trailing slop.

## Detailed Blueprint
- **Modified**: `src/app/api/ai/brain/analyze/route.ts`
  - Added `extractJsonArray()` function to accurately scan and return exactly the valid JSON array from the response.

## Operational Trace
- Successfully verified error prevention.

## Status Assessment
Balanced bracket JSON extraction successfully eliminates parsing errors due to trailing commentary.
