User request: "instead of showing link in text format, show all the sources used for the answer generating in the end like pills with urls"

## Objective Reconstruction
The user requested to remove inline links from the answer text and instead display all generated search source URLs at the very end of the answer as styled pill components.

## Strategic Reasoning
To achieve this, we:
1. Updated the AI synthesis prompts to prohibit inline links in the body and enforce generating all source links at the end under a dedicated `**Sources:**` line.
2. Custom-designed the ReactMarkdown anchor component renderer in the UI (`ChatMessage.tsx`) to render markdown links as sleek, custom pills with badges.

## Detailed Blueprint
- **Modified**: `src/lib/bot/providers/tavily.ts`
  - Re-prompted the AI to output clean body text and format sources at the end.
- **Modified**: `src/lib/bot/providers/duckduckgo.ts`
  - Applied the same prompting rules for consistent behavior.
- **Modified**: `src/components/assistant/components/ChatMessage.tsx`
  - Implemented custom `a` tag component renderer for pills layout.

## Operational Trace
- Successfully tested the search chain and confirmed it outputs links in pill format at the end.

## Status Assessment
Markdown links are now displayed as beautiful pills.
