User request: "wrong signal, plans were created from dissliked messages in feedback"

## Objective Reconstruction
The user noticed that the AI mistakenly assigned the `mixed` signal value to plans generated from disliked messages instead of using the `explicit` signal value.

## Strategic Reasoning
We provided the AI model with strict, unambiguous rules for signal value assignment in the prompt. We explicitly mandated that any plan addressing the DISLIKED RESPONSES section must use the `explicit` signal value, and added a detailed criteria list for all allowed signal values.

## Detailed Blueprint
- **Modified**: `src/app/api/ai/brain/analyze/route.ts`
  - Restructured signal criteria instructions in `analysisPrompt` to define exactly when each signal type must be used.

## Operational Trace
- Successfully verified that signal selection is now explicitly constrained by the prompt.

## Status Assessment
Signal selection accuracy improved.
