User request: "change signals, reomve all and add only 2 new, that show plan sources, feedback analysis or routine run and whether its manual or auto"

## Objective Reconstruction
The user requested to remove the four old signal types and replace them with exactly two new ones:
1. Source type: `feedback analysis` vs `routine run`
2. Trigger type: `manual` vs `auto`

## Strategic Reasoning
By utilizing the existing `triggered_by` attribute stored in the database's `bot_analysis_sessions` table, we can dynamically and accurately populate the plan's `source` and `trigger` attributes before returning them to the frontend. We created two clean new tag categories in `RoutineClient.tsx` to completely replace the previous signal legend and rendering code.

## Detailed Blueprint
- **Modified**: `src/app/admin/bot/routine/planActions.ts`
  - Added `source` and `trigger` fields to the `ImprovementPlan` interface.
  - Mapped `triggered_by` to the returned plans in `getLatestPlans()`.
- **Modified**: `src/app/api/ai/brain/analyze/route.ts`
  - Directly added the mapped `source` and `trigger` attributes when pushing plans to complete event.
- **Modified**: `src/app/admin/bot/routine/RoutineClient.tsx`
  - Replaced the old `SIGNAL_CONFIG` with `SOURCE_CONFIG` and `TRIGGER_CONFIG`.
  - Replaced the signal legend and individual plan card badges.

## Operational Trace
- Scoped source type and trigger type badges across all plan cards.

## Status Assessment
New Source and Trigger signal badges are fully operational.
